require.config({
  baseUrl: "../www/js/",
  paths: {
    underscore: 'lib/underscore.amd',
    text: 'lib/text'
  }
});

var tests = [
  '../test/tests/basic.js'
  //'../test/tests/cache.js'
];

mocha.setup('bdd');
mocha.setup({globals: ['bookstrap']});

require(tests, function() {
  mocha.run();
});